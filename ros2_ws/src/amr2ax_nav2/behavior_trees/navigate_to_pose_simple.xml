<!--
  BT simplu pentru NavigateToPose fara replanning la 1Hz.
  Elimina timeout-ul la compute_path/follow_path din BT-ul default cu replanning.
  
  Diferenta fata de BT-ul default (navigate_to_pose_w_replanning_and_recovery.xml):
  - Planifica o singura data (fara RateController pe ComputePathToPose)
  - FollowPath ruleaza pana la goal fara a fi intrerupt de replanificari
  - Recovery inclus: ClearCostmap + Spin + Wait + BackUp
  
  Deploy: /home/pi/saim_xplorer/config/navigate_to_pose_simple.xml
  Referinta in xplorer.yaml: default_nav_to_pose_bt_xml
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="3" name="NavigateRecovery">
      <Sequence name="NavigateWithSinglePlan">
        <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"
                           error_code_id="{compute_path_error_code}"/>
        <FollowPath path="{path}" controller_id="FollowPath"
                    error_code_id="{follow_path_error_code}"/>
      </Sequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <Sequence name="ClearCostmaps">
            <ClearEntireCostmap name="ClearLocalCostmap"
              service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap name="ClearGlobalCostmap"
              service_name="global_costmap/clear_entirely_global_costmap"/>
          </Sequence>
          <Spin spin_dist="1.57" error_code_id="{spin_error_code}"/>
          <Wait wait_duration="5.0"/>
          <BackUp backup_dist="0.30" backup_speed="0.05"
                  error_code_id="{backup_error_code}"/>
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
